#include <stdio.h>
int main(){
    int contador = 0;

        for ( int num = 1; num < 5; num++){

            contador = contador + 3;
            
        printf("Os numeros são: %d\n",contador);
}

return 0;
}